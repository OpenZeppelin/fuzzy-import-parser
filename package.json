{
  "name": "@openzeppelin/fuzzy-solidity-import-parser",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "tsc && ava",
    "test:watch": "concurrently -krs first -n ava,tsc 'ava --watch' 'tsc --watch --preserveWatchOutput'",
    "prepare": "npm run build",
    "prepublishOnly": "rm -rf dist"
  },
  "files": [
    "dist/**/*.js"
  ],
  "author": "OpenZeppelin",
  "license": "MIT",
  "devDependencies": {
    "ava": "^2.4.0",
    "code-style": "github:OpenZeppelin/code-style",
    "concurrently": "^5.0.0",
    "typescript": "^3.7.2"
  }
}
